// Solution for: https://projecteuler.net/problem=2

include "std.phtn"

1 1 while dup 4000000 < do
    dup 2 % 0 == if
        dup mem ,64 +
        mem swap .64
    end
    swap over +
end

mem ,64 print